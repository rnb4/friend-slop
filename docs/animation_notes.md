- Use rigify to make rig (APPLY SCALE/TRANSFORMS FIRST) using generate rig button in rigify armature
- for mixamo set body type to 100
- when making edits to character and exporting character mesh, apply modifiers and select "only renderable objects" for export
- when importing animation from mixamo, set bone map in advanced import on skeleton and make sure to select import 
as animation library in basic import tab
- character is put in assets and saved to actors as inherited scene where the game-related stuff is added
